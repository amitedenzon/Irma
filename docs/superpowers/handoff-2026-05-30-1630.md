# Handoff — Apple Reminders sync (2026-05-30, mid-afternoon pause)

**Resume target:** 2026-05-30 20:00 Asia/Jerusalem.

## Where you are

You're the controller running `superpowers:subagent-driven-development` for the Apple Reminders two-way sync feature, in a **worktree** to avoid colliding with a concurrent Claude session on the main checkout.

- Worktree: `/Users/amit/Documents/Code/Irma/.claude/worktrees/feat+reminders-sync`
- Branch: `worktree-feat+reminders-sync` (cut from `origin/main`, spec/plan cherry-picked on top)
- Commits so far (newest → oldest):

  | SHA | Subject |
  |---|---|
  | `df06234` | docs(plans): partial amendment for one-list-per-project (Tasks 8-11) |
  | `d9e74ad` | feat(reminders): refit helper for one-list-per-project architecture |
  | `7ad1abd` | docs(specs): amend — one Reminders list per Project |
  | `dc80b5e` | docs(plans): task 6 verification gate `swift build` (post-amendment) |
  | `f5cf301` | feat(reminders): list / batch / delete-calendar handlers |
  | `1102b5d` | feat(reminders): handler for access-status / request-access / ensure-list |
  | `8197efb` | feat(reminders): RemindersClient protocol |
  | `c9a0a6c` | feat(reminders): codable JSON DTOs for the helper |
  | `04b7adc` | feat(reminders): scaffold Swift helper package |
  | `ea181d3` | docs(plans): amend tasks 1-5 — drop swift unit tests |

## Two amendments to know

1. **XCTest dropped (commit `ea181d3`)** — Xcode CLT doesn't ship XCTest; Swift verification is `swift build` only. Affects Tasks 1–5.
2. **One list per project (commit `7ad1abd`, refit `d9e74ad`)** — public EventKit has no `parentReminder` API; the original "Project = parent reminder, Task = subtask" design is impossible. Now: **each Project = an `EKCalendar` named `Irma · <ProjectName>`; Tasks are flat reminders in their project's calendar.** New helper commands: `list-calendars --prefix`, `rename-calendar`. New Project linkage column: `reminder_calendar_id` (not `reminder_uuid`).

## Task tracker state

- Tasks 1–6 **done** under the new architecture (the Swift helper is complete and builds clean).
- Tasks 7–22 **pending**. The Python side hasn't been touched.

## Plan-amendment state

The post-amendment plan rewrite is **partial**:

| Task | Amendment status |
|---|---|
| 7  | unaffected — main.swift dispatch is generic |
| 8  | ✅ amended (drop `parent_uuid`, add `CalendarSummary`) |
| 9  | ✅ amended (note added — implementer adds `list_calendars` + `rename_calendar` bridge methods + fake-helper handlers) |
| 10 | ✅ amended (`Project.reminder_calendar_id` column, not `reminder_uuid`) |
| 11 | ✅ amended (`Project.reminder_calendar_id` + `set_reminder_calendar_id` repo method) |
| 12 | ❌ **NEEDS WHOLESALE REWRITE.** The planner's signature, dataclasses, and algorithm all change. New: `plan(irma_snapshot, helper_state) -> SyncPlan` where `helper_state` is `{calendar_id: (title, [HelperReminder])}`. SyncPlan now carries calendar-level ops (`CreateCalendar`, `RenameCalendar`, `DeleteCalendar`) in addition to per-reminder ones. Test cases must cover: project-needs-calendar, calendar-rename-on-phone-renames-project, prefix-dropped-on-phone-unlinks-project, task-both-sides-match, task-irma-only, task-remote-only-into-its-project, conflict-by-timestamp, cross-calendar-task-move, paused-project-prefix-rename, archived-project-deletes-calendar. |
| 13 | unaffected — `ensure_inbox_project` is unchanged |
| 14 | ❌ **NEEDS WHOLESALE REWRITE.** `_apply` becomes a four-pass algorithm: (1) calendar reconcile via `ensure_list` / `rename_calendar` / `delete_calendar`; (2) per-project `helper list --calendar-id <id>` snapshot; (3) per-project reminder reconcile; (4) per-project `batch` apply. Writes back `reminder_calendar_id` (projects) and `reminder_uuid` (tasks). Service test cases largely the same shape but the SyncStats need new counters (`created_calendars`, `renamed_calendars`, `deleted_calendars`). |
| 15 | ❌ amended preamble required: drop `reminders_calendar_id`, add `reminders_linked: bool = False` and `reminders_calendar_prefix: str = "Irma · "`. |
| 16 | unaffected |
| 17 | ❌ amended preamble required: link flow no longer calls `ensure-list("Irma")`. It calls `request-access`, sets `reminders_linked=True`, triggers `sync_once()` which handles per-project ensure-list via the planner. Unlink clears `reminder_calendar_id` from projects and `reminder_uuid` from tasks. |
| 18 | unaffected |
| 19 | unaffected |
| 20 | unaffected |
| 21 | ❌ amended preamble required: e2e test creates **per-project** calendars (`Irma-Test-Alpha-<uuid>`, `Irma-Test-Inbox-<uuid>`). Tears them down individually. |
| 22 | ❌ amended preamble required: README's "Apple Reminders sync" section explains the `Irma · <Project>` naming convention and what shows up in the phone Reminders sidebar (a list per project + the Inbox). |

## What to do on resume

In this order:

1. **Verify state:** confirm worktree clean (`git status`), branch matches, tip is `df06234`.
2. **Finish the plan amendment:**
   - Wholesale-rewrite Task 12 (planner) in the plan doc. The hardest part — design the new dataclasses and per-test-case scenarios up front. The Swift `helper list-calendars` and `helper list --calendar-id` return shapes are settled (see `d9e74ad`).
   - Wholesale-rewrite Task 14 (sync service apply phases).
   - Add short amendment-preambles to Tasks 15, 17, 21, 22.
   - Commit as a single `docs(plans): complete one-list-per-project amendment` commit.
3. **Resume subagent dispatch from Task 7.** Mark Task 7 in-progress; dispatch implementer; review; mark done; on to Task 8.
4. **Continue Tasks 8–22 sequentially.** Each subagent gets the task's verbatim text from the (now fully amended) plan, plus the standing context: stay in this worktree, commits go to `worktree-feat+reminders-sync` only.

## Caveat: orphan commit on the user's other branch

A Task 6 implementer earlier today escaped the worktree and committed `9553173` (an old-architecture, parent-reminder-flavored `EventKitRemindersClient.swift`) to **`feat/chat-tools-parity`** instead of this worktree. The user has been told. They may have reverted it on their side; check before doing anything that touches that branch.

The canonical, new-architecture `EventKitRemindersClient.swift` lives here in commit `d9e74ad`.

## Subagent dispatch lessons learned

- The "stay in `tools/reminders-helper/`, do not switch branches" instruction got ignored by one implementer. Future dispatches must reinforce: explicitly state the worktree path, prefix `git` commands with absolute paths when possible, and verify the commit landed on the expected SHA in the controller before accepting DONE.
- Two implementers added unauthorized small deviations (`.writeOnly` enum case, redundant comments). These compiled fine but were not in spec. Spec-compliance reviewer caught them on inspection; consider tightening the implementer prompt's "verbatim from plan" wording.

## Final-stage tasks (after Tasks 7–22 ship)

Already in the tracker:
- Task 31: full-branch code review (single comprehensive review subagent over `origin/main..HEAD`).
- Task 32: invoke `superpowers:finishing-a-development-branch`.
